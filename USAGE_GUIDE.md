# 📖 Руководство по использованию системы

## 🚀 Быстрый старт

### 1. Запуск системы
```bash
# Простой запуск
start-simple.bat

# Доступ к системе
http://localhost:8847
```

### 2. Вход в систему
- **Telegram ID**: `123456789`
- **Ключ доступа**: `simple-admin-key-2026`

## 👥 Управление пользователями

### Регистрация нового пользователя

1. Откройте вкладку **"Пользователи"**
2. Нажмите кнопку **"Добавить пользователя"**
3. Заполните форму:
   - **Telegram ID** - уникальный ID пользователя в Telegram
   - **Полное имя** - ФИО сотрудника
   - **Телефон** - контактный номер
   - **Роль** - выберите тип пользователя:
     - `courier` - курьер/водитель
     - `passenger` - пассажир/клиент
     - `admin` - администратор
   - **Филиал** - выберите из списка
   - **Адрес** - адрес проживания/работы

4. Нажмите **"Сохранить"**

### Данные сохраняются в базе данных

✅ **Автоматическое сохранение:**
- Все данные сохраняются в базе данных (LowDB или Nhost)
- Информация доступна сразу после регистрации
- Данные синхронизируются между всеми компонентами системы

✅ **Использование данных:**
- **Смены** - создаются на основе зарегистрированных пользователей
- **Маршруты** - назначаются курьерам и пассажирам из базы
- **Telegram бот** - находит пользователей по Telegram ID
- **Статистика** - рассчитывается на основе данных из БД

## 📊 Работа с данными

### Просмотр информации о базе данных

1. Откройте вкладку **"База данных"**
2. Здесь вы увидите:
   - **Тип хранилища** - LowDB (JSON) или Nhost (PostgreSQL)
   - **Статус синхронизации** - включена/отключена
   - **Статистику данных** - количество пользователей, курьеров, пассажиров
   - **Состояние хранилищ** - готовность LowDB и Nhost

### Переключение типа хранилища

1. В разделе **"База данных"** нажмите **"Переключить хранилище"**
2. Выберите тип:
   - **LowDB** - JSON файлы (быстрый старт, до 500 пользователей)
   - **Nhost** - PostgreSQL (масштабируемость, неограниченно)

## 🔄 Смены и назначения

### Создание смены

1. Откройте вкладку **"Смены"**
2. Нажмите **"Добавить в смену"** рядом с пользователем
3. Пользователь автоматически добавляется в смену на сегодня

### Создание маршрута

1. Откройте вкладку **"Маршруты"**
2. Нажмите **"Создать маршрут"**
3. Выберите:
   - **Курьера** - из списка пользователей в смене
   - **Пассажира** - из списка пользователей в смене
   - **Время** - время выполнения маршрута
   - **Адреса** - откуда и куда

## 🤖 Telegram бот

### Настройка бота

1. Создайте бота через @BotFather в Telegram
2. Получите токен
3. Добавьте токен в файл `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
   ```
4. Перезапустите систему

### Использование бота

Пользователи могут:
- Получать уведомления о новых маршрутах
- Подтверждать выполнение заданий
- Просматривать свои смены
- Получать информацию о статусе

## 📈 Мониторинг системы

### Автообновление данных

1. В Dashboard включите **"Автообновление"**
2. Данные будут обновляться каждые 30 секунд
3. Счетчик показывает время до следующего обновления

### Статистика в реальном времени

Dashboard показывает:
- **Всего сотрудников** - количество зарегистрированных пользователей
- **Сотрудников в смене** - активных смен сегодня
- **Активных маршрутов** - запланированных поездок
- **Курьеров в работе** - доступных для заказов

## 🔧 Настройки системы

### Переменные окружения (.env)

```env
# Основное хранилище данных
PRIMARY_STORAGE=lowdb          # JSON файлы
PRIMARY_STORAGE=nhost          # PostgreSQL

# Синхронизация
NHOST_SYNC_ENABLED=false       # Отключена
NHOST_SYNC_ENABLED=true        # Включена

# Telegram бот
TELEGRAM_BOT_TOKEN=your-token

# Nhost настройки
NHOST_SUBDOMAIN=your-subdomain
NHOST_REGION=eu-central-1
```

### Режимы работы

**Режим разработки (по умолчанию):**
```env
PRIMARY_STORAGE=lowdb
NHOST_SYNC_ENABLED=false
```

**Продакшн режим:**
```env
PRIMARY_STORAGE=nhost
NHOST_SYNC_ENABLED=true
```

## 💾 Резервное копирование

### Автоматическое (LowDB)
Данные сохраняются в папке `backend-json/data/`:
- `users.json` - все пользователи
- `branches.json` - филиалы
- `shifts.json` - смены
- `assignments.json` - маршруты

### Ручное копирование
```bash
xcopy backend-json\data backup\%date% /E /I
```

### Облачное (Nhost)
При использовании Nhost данные автоматически сохраняются в облаке.

## 🚨 Решение проблем

### Пользователь не сохраняется
1. Проверьте уникальность Telegram ID
2. Убедитесь что все обязательные поля заполнены
3. Проверьте статус базы данных в разделе "База данных"

### Данные не отображаются
1. Нажмите кнопку "Синхронизировать" в Dashboard
2. Проверьте подключение к серверу (http://localhost:8848/health)
3. Перезапустите систему через `stop-project.bat` и `start-simple.bat`

### Telegram бот не работает
1. Проверьте токен в файле `.env`
2. Убедитесь что бот создан через @BotFather
3. Перезапустите сервер

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь что порты 8847 и 8848 свободны
3. Проверьте статус сервисов через health check
4. Обратитесь к документации в `README.md`

---

**Система готова к использованию!** 🎉

Все данные автоматически сохраняются в базе данных и доступны для всех компонентов системы.